type Circle {
  id: ID!
  chatID: ID!
  name: String!
  lastMessageContent: String!
  lastMessageAt: Time!
  createdAt: Time!
  messageDatePartitions: [MessageDatePartition]!
}

input createCircleInput {
  chatID: ID!
  name: String!
}

input updateCircleInput {
  id: ID!
  name: String
}
