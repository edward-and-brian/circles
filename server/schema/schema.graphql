schema {
  query: Query
  mutation: Mutation
}

type Query {
  User(id: ID!): User
  Users(): [User]!

  # circle(id: String): Circle

  # chat(id: String): Chat
}

type Mutation {
  createUser(input: createUserInput!): User
  updateUser(input: updateUserInput!): User
  deleteUser(id: ID!): User

  # createCircle(chatID: String!, name: String!): Circle
  # updateCircle(id: String!, name: String): Circle
  # deleteCircle(id: String!): Circle

  # createChat(name: String!): Chat
  # updateChat(id: String!, name: String): Chat
  # deleteChat(id: String!): Chat

  # createMessages(circleID: String!, content: String!, senderID: String!): Message
  # updateMessages(id: String!, content: String): Message
  # deleteMessages(id: String!): Message
}

scalar Time
