type Circle {
  id: ID!
  chatID: ID!
  name: String!
  createdAt: Time!
  messages: [Message]!
}

input createCircleInput {
  chatID: ID!
  name: String!
}

input updateCircleInput {
  id: ID!
  name: String!
}
