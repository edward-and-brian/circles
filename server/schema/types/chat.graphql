type Chat {
  id: ID!
  name: String!
  createdAt: Time!
  circles: [Circle]!
  users: [User]!
  lastCircleName: String!
  lastMessageAt: Time!
}

input createChatInput {
  name: String!
}

input updateChatInput {
  id: ID!
  name: String
}
